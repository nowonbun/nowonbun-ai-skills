# 수정 요약

- `mariadb-mcp.md`를 현재 MariaDB MCP 권한과 SQL 안전 규칙에 맞는 영문 실행 규칙 문서로 재작성하고 companion readme를 추가했다.

# 원인

- 기존 문서는 매우 짧고 설명형이었으며 현재 MCP 권한 경계와 single-statement/read-only 규칙이 충분히 드러나지 않았다.

# 변경 파일

- `D:/work/nowonbun-harness/codex-skills/tool-usage-management/mariadb-mcp.md`
- `D:/work/nowonbun-harness/doc/codex-skills/tool-usage-management/mariadb-mcp-readme.md`

# 변경 내용

- connection scope, permission boundary, query safety, schema assumption 처리 규칙을 추가했다.
- write 금지와 `unverified` 표기 규칙을 분리했다.

# 재발 방지

- DB MCP 스킬 수정 시 실제 MCP permission 응답과 문서 규칙이 일치하는지 먼저 검증한다.
- 스키마 추정은 증거 없으면 사실처럼 적지 않는다.

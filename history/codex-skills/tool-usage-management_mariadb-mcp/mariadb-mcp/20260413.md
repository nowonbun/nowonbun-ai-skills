# 수정 요약

- mariadb-mcp의 Source of Truth 경계를 보완했다.

# 원인

- 기존 문구가 데이터베이스 고유 판단과 workspace 공통 MCP 제어의 경계를 충분히 설명하지 않았다.

# 변경 파일

- D:/work/nowonbun-harness/codex-skills/tool-usage-management/mariadb-mcp.md
- D:/work/nowonbun-harness/doc/codex-skills/tool-usage-management/mariadb-mcp-readme.md

# 변경 내용

- connection-scope check, SQL validation, permission handling을 직접 범위로 명시했다.
- `AGENTS`와 `work-runtime`의 위임 경계를 각각 workflow stop 조건과 공통 MCP 검증으로 분리했다.
- 단일 주제 H2였던 `# Must NOT`와 `# Flow` 내부 제목을 제거하고 항목을 H1 직하로 이동했다.

# 재발 방지

- 데이터베이스 고유 검증과 공통 workflow 제어를 구분해 적는다.

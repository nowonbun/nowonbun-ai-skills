# 수정 요약

- vocabulary-rule의 잘못된 source-of-truth 경로와 history 경로를 project-root-relative 기준으로 수정했다.

# 원인

- registry, checklist, history 경로가 현재 저장소 구조가 아니라 과거 경로 체계를 가리키고 있었다.

# 변경 파일

- D:/work/nowonbun-harness/codex-skills/vocabulary-management/vocabulary-rule.md
- D:/work/nowonbun-harness/codex-skills/vocabulary-management/vocabulary-registry.md

# 변경 내용

- active registry 경로를 `codex-skills/vocabulary-management/vocabulary-registry.md`로 수정했다.
- terminology checklist 경로를 `codex-skills/vocabulary-management/vocabulary-terminology-conflict-checklist.md`로 수정했다.
- history path를 `history/codex-skills/vocabulary-management/vocabulary-rule/`로 수정했다.
- registry 파일명을 `vocaburaly-registry.md`에서 `vocabulary-registry.md`로 정정했다.

# 재발 방지

- source-of-truth 경로를 수정할 때는 문서 선언과 실제 저장소 경로를 함께 대조한다.
- skill 문서 간 참조는 file path 대신 frontmatter `name` 기준을 우선 사용하고, 비스킬 산출물만 실제 경로를 사용한다.
